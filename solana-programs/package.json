{
  "name": "@dpo2u/solana-programs",
  "version": "0.1.0",
  "private": true,
  "description": "DPO2U 5 Anchor programs — scaffolded in Sprint 3, deployed in Sprint 4.",
  "type": "module",
  "scripts": {
    "build": "anchor build",
    "pretest": "mkdir -p tests/fixtures && cp -f ../sp1-solana/target/deploy/dpo2u_compliance_verifier.so tests/fixtures/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ledger": "anchor test",
    "clean": "anchor clean && rm -rf target .anchor"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1",
    "@sqds/multisig": "^2.1.3",
    "bn.js": "^5.2.3",
    "litesvm": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "solana-bankrun": "^0.4.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
